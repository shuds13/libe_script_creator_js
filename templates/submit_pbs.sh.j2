#!/bin/bash -l
#PBS -l select={{ total_nodes }}
#PBS -l walltime=00:15:00
#PBS -q <queue_name>
#PBS -A <myproject>

{{#num_gpus}}
export MPICH_GPU_SUPPORT_ENABLED=1
{{/num_gpus}}

cd $PBS_O_WORKDIR
python run_libe.py
